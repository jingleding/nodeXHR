define(function(require, exports, module) {

  #!	/usr	/bin/env node
  
  var mime = require('node_modules/mime/mime');
  var file = process.argv[2];
  var type = mime.lookup(file);
  
  process.stdout.write(type + '\n');
  
  

});